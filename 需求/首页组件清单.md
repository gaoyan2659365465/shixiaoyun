# 首页组件快速参考清单

## 📦 组件总览

| 组件名称 | 文件路径 | 用途 | 状态 |
|---------|---------|------|------|
| SearchBar | `pages/index/components/SearchBar.vue` | 搜索框 | 待创建 |
| BannerSwiper | `pages/index/components/BannerSwiper.vue` | 轮播横幅 | 待创建 |
| QuickEntry | `pages/index/components/QuickEntry.vue` | 快捷入口 | 待创建 |
| SectionTitle | `pages/index/components/SectionTitle.vue` | 区块标题 | 待创建 |
| ActivityCard | `pages/index/components/ActivityCard.vue` | 活动卡片 | 待创建 |

---

## 🔍 SearchBar 组件

### 基本信息
- **用途**: 顶部搜索框，点击跳转搜索页
- **尺寸**: 高度 56rpx
- **位置**: 页面顶部

### Props
```javascript
placeholder: String = 'Search'
```

### Events
```javascript
@click // 点击搜索框
```

### 关键样式
- 圆角: 16rpx
- 背景: #FFFFFF
- 边距: 左右32rpx, 上下24rpx

---

## 🎠 BannerSwiper 组件

### 基本信息
- **用途**: 图片轮播展示
- **尺寸**: 高度 280rpx
- **功能**: 自动播放、指示点、点击跳转

### Props
```javascript
banners: Array (必需)
autoplay: Boolean = true
interval: Number = 3000
duration: Number = 500
```

### Events
```javascript
@click // 点击横幅，传递 {banner, index}
```

### 数据结构
```javascript
banner: {
  id: Number,
  image: String,
  title: String,
  subtitle: String,
  link: String
}
```

### 关键样式
- 圆角: 16rpx
- 边距: 左右32rpx
- 指示点: 白色/半透明

---

## 🚀 QuickEntry 组件

### 基本信息
- **用途**: 快捷入口图标导航
- **数量**: 3个图标
- **布局**: 水平均分

### Props
```javascript
entries: Array (必需)
```

### Events
```javascript
@click // 点击入口，传递 entry 对象
```

### 数据结构
```javascript
entry: {
  id: Number,
  icon: String,
  title: String,
  path: String,
  badge: String (可选)
}
```

### 关键样式
- 图标容器: 120rpx × 120rpx 圆形
- 图标: 64rpx × 64rpx
- 背景: #FFFFFF
- 阴影: 0 4rpx 16rpx rgba(0,0,0,0.08)

---

## 📌 SectionTitle 组件

### 基本信息
- **用途**: 区块标题栏
- **功能**: 显示标题、副标题、右箭头

### Props
```javascript
title: String (必需)
subtitle: String = ''
showArrow: Boolean = true
```

### Events
```javascript
@click // 点击标题区域
```

### 关键样式
- 高度: 88rpx
- 标题字体: 32rpx, 字重500
- 副标题字体: 24rpx
- 箭头: 32rpx × 32rpx

---

## 🎯 ActivityCard 组件

### 基本信息
- **用途**: 活动信息卡片
- **功能**: 展示活动、报名按钮

### Props
```javascript
activity: Object (必需)
```

### Events
```javascript
@click-card // 点击卡片主体
@click-button // 点击操作按钮
```

### 数据结构
```javascript
activity: {
  id: Number,
  image: String,
  title: String,
  startDate: String,
  endDate: String,
  status: String, // 'ongoing'|'upcoming'|'ended'
  participants: Number,
  maxParticipants: Number
}
```

### 状态样式
| 状态 | 按钮背景 | 按钮文字 | 文案 |
|------|---------|---------|------|
| ongoing | #17171a | #FFFFFF | 报名参加 |
| upcoming | #4458ff | #FFFFFF | 即将开始 |
| ended | #E5E6EA | #a7abb2 | 已结束 |

### 关键样式
- 圆角: 16rpx
- 图片高度: 400rpx
- 内边距: 24rpx
- 按钮高度: 64rpx

---

## 📊 组件使用示例

### 在首页中使用

```vue
<template>
  <view class="home-page">
    <!-- 搜索框 -->
    <SearchBar @click="handleSearch" />
    
    <!-- 轮播横幅 -->
    <BannerSwiper 
      :banners="banners" 
      @click="handleBannerClick" 
    />
    
    <!-- 快捷入口 -->
    <QuickEntry 
      :entries="quickEntries" 
      @click="handleEntryClick" 
    />
    
    <!-- 活动区域 -->
    <SectionTitle 
      title="活动" 
      @click="handleMoreActivities"
    />
    
    <ActivityCard
      v-for="activity in activities"
      :key="activity.id"
      :activity="activity"
      @click-card="handleActivityDetail"
      @click-button="handleJoinActivity"
    />
  </view>
</template>

<script>
import SearchBar from './components/SearchBar.vue'
import BannerSwiper from './components/BannerSwiper.vue'
import QuickEntry from './components/QuickEntry.vue'
import SectionTitle from './components/SectionTitle.vue'
import ActivityCard from './components/ActivityCard.vue'

export default {
  components: {
    SearchBar,
    BannerSwiper,
    QuickEntry,
    SectionTitle,
    ActivityCard
  },
  // ...
}
</script>
```

---

## 🎨 样式变量参考

```scss
// 在组件中使用
@import '@/styles/variables.scss';

.my-component {
  background-color: $bg-color;
  padding: $spacing-4;
  
  .title {
    font-size: $font-t2;
    color: $text-title;
  }
}
```

### 可用变量

**颜色**:
- `$brand-blue`: #4458ff
- `$bg-color`: #F5F6FA
- `$card-bg`: #FFFFFF
- `$text-title`: #17171a
- `$text-body`: #656a73
- `$text-secondary`: #a7abb2

**间距**:
- `$spacing-1`: 8rpx
- `$spacing-2`: 16rpx
- `$spacing-3`: 24rpx
- `$spacing-4`: 32rpx

**字体**:
- `$font-t2`: 36rpx
- `$font-t3`: 32rpx
- `$font-t4`: 28rpx
- `$font-t5`: 24rpx

---

## 🔧 开发提示

### 1. 组件创建顺序
建议按以下顺序创建组件：
1. SearchBar（最简单）
2. SectionTitle（简单）
3. QuickEntry（中等）
4. ActivityCard（中等）
5. BannerSwiper（复杂）

### 2. 调试技巧
- 使用 `console.log` 查看 props 数据
- 使用微信开发者工具的调试器
- 检查组件是否正确导入和注册

### 3. 常见错误
- ❌ 忘记在父组件中导入子组件
- ❌ Props 数据格式不匹配
- ❌ 事件名称拼写错误
- ❌ 样式变量未导入

### 4. 性能优化
- 使用 `v-if` 而非 `v-show` 对于不常切换的内容
- 为列表项添加唯一 `key`
- 图片使用懒加载
- 避免在模板中使用复杂计算

---

## 📝 检查清单

创建每个组件后，请检查：

- [ ] 组件文件已创建在正确路径
- [ ] Props 定义完整且有默认值
- [ ] Events 正确触发
- [ ] 样式符合设计规范
- [ ] 响应式布局正常
- [ ] 在父组件中正确导入
- [ ] 在父组件中正确注册
- [ ] 在模板中正确使用
- [ ] 数据传递正确
- [ ] 事件处理正确

---

## 🎯 快速开始

### 最小可运行版本

如果想快速看到效果，可以先创建简化版本：

1. **创建 SearchBar**（5分钟）
2. **创建 SectionTitle**（5分钟）
3. **创建简化的 ActivityCard**（10分钟）
4. **在首页中使用**（5分钟）

总计约 25 分钟即可看到基本效果。

然后再逐步完善：
- 添加 BannerSwiper
- 添加 QuickEntry
- 完善样式细节
- 添加交互功能

---

## 📚 相关文档

- [首页实现方案.md](./首页实现方案.md) - 详细设计方案
- [首页组件实现代码.md](./首页组件实现代码.md) - 完整代码
- [首页实施指南.md](./首页实施指南.md) - 实施步骤

---

**提示**: 建议打印此清单，在开发过程中随时查阅！ 📋